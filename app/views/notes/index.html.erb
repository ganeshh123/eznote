<%= link_to 'New Note', new_note_path%>
<% @notes.each do |note| %>
    <h2><%= link_to note.title, '/notes/'+note.id.to_s %></h2>
    <h3><%= time_ago_in_words(note.created_at) %></h3>
    <p><%= note.content %></p>
<% end %>